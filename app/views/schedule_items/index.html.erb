<p id="notice"><%= notice %></p>

<ul class="collection with-header">
    <li class="collection-header">
        <h4>Today&apos;s activities</h4>
    </li>
    <% @schedule_items.each do |schedule_item| %>
            <li class="collection-item">
                  <span class="title"><%= schedule_item.scheduledDate %> <%= schedule_item.title %></span>
                  <div class="row">
                      <div class="col s7">
                          <p class="blue-grey-text left"><%= schedule_item.subtitle %></p>
                      </div>
                      <div class="col s5">
                          <a class="btn blue lighten-1 right">Check in<i class="material-icons right">beenhere</i></a>
                      </div>
                      <div class="col s12">
                              <hr/>
                      </div>
                  </div>
            </li>
            <!--
            <%= link_to 'New Schedule Item', new_schedule_item_path %>
        <td><%= link_to 'Show', schedule_item %></td>
        <td><%= link_to 'Edit', edit_schedule_item_path(schedule_item) %></td>
        <td><%= link_to 'Destroy', schedule_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        -->
    <% end %>
  </ul>
